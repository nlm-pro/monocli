#!/usr/bin/env node
'use strict';

try {
  process.title = 'monocli ' + Array.from(process.argv).slice(2).join(' ');
} catch(_) {
  process.title = 'monocli';
}

const monocli = require('../dist/index');
monocli.run(process.argv.slice(2))
